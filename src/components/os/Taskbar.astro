---
import type { Lang } from '../../i18n/utils';

interface Props {
  lang: Lang;
  alternateLangPath: string;
}

const { lang, alternateLangPath } = Astro.props;
const alternateLangLabel = lang === 'fr' ? 'EN' : 'FR';
const alternateLangAriaLabel = lang === 'fr' ? 'Switch to English' : 'Passer en français';
const startLabel = lang === 'fr' ? 'Albert.exe — Menu principal' : 'Albert.exe — Main menu';
---

<!-- Start Menu popup -->
<div
  id="start-menu"
  class="start-menu"
  hidden
  role="dialog"
  aria-label={lang === 'fr' ? 'Menu principal' : 'Main menu'}
  aria-modal="false"
>
  <!-- Header -->
  <div class="start-menu__header">
    <div class="start-menu__avatar" aria-hidden="true">⚡</div>
    <div class="start-menu__identity">
      <p class="start-menu__name">Albert Lecomte</p>
      <p class="start-menu__role">{lang === 'fr' ? 'Concepteur Designer UI' : 'UI Designer Conceptor'}</p>
    </div>
  </div>

  <!-- Section: navigation -->
  <ul class="start-menu__section" role="list">
    <li>
      <button class="start-menu__item" type="button" data-start-open="about">
        <span class="start-menu__item-icon" aria-hidden="true">👤</span>
        <span>{lang === 'fr' ? 'À propos' : 'About'}</span>
      </button>
    </li>
    <li>
      <button class="start-menu__item" type="button" data-start-open="projects">
        <span class="start-menu__item-icon" aria-hidden="true">📁</span>
        <span>{lang === 'fr' ? 'Projets' : 'Projects'}</span>
      </button>
    </li>
    <li>
      <button class="start-menu__item" type="button" data-start-open="contact">
        <span class="start-menu__item-icon" aria-hidden="true">📧</span>
        <span>Contact</span>
      </button>
    </li>
    <li>
      <a class="start-menu__item" href="/cv/albert-lecomte-cv.pdf" download>
        <span class="start-menu__item-icon" aria-hidden="true">📄</span>
        <span>{lang === 'fr' ? 'Télécharger CV' : 'Download CV'}</span>
      </a>
    </li>
  </ul>

  <div class="start-menu__sep" role="separator"></div>

  <!-- Section: legal -->
  <ul class="start-menu__section start-menu__section--legal" role="list">
    <li class="start-menu__section-label" aria-hidden="true">
      {lang === 'fr' ? 'Informations légales' : 'Legal'}
    </li>
    <li>
      <a class="start-menu__item start-menu__item--legal" href="/mentions-legales">
        <span class="start-menu__item-icon" aria-hidden="true">⚖️</span>
        <span>{lang === 'fr' ? 'Mentions légales' : 'Legal notice'}</span>
      </a>
    </li>
    <li>
      <a class="start-menu__item start-menu__item--legal" href="/politique-confidentialite">
        <span class="start-menu__item-icon" aria-hidden="true">🔒</span>
        <span>{lang === 'fr' ? 'Politique de confidentialité' : 'Privacy policy'}</span>
      </a>
    </li>
    <li>
      <a class="start-menu__item start-menu__item--legal" href="/declaration-accessibilite">
        <span class="start-menu__item-icon" aria-hidden="true">♿</span>
        <span>{lang === 'fr' ? 'Accessibilité' : 'Accessibility'}</span>
      </a>
    </li>
  </ul>

  <div class="start-menu__footer">
    <span aria-hidden="true">🌐</span>
    lecomte-albert-cv.vercel.app
  </div>
</div>

<nav class="taskbar" role="navigation" aria-label={lang === 'fr' ? 'Barre des tâches' : 'Taskbar'}>
  <!-- Left: Start button -->
  <button
    class="taskbar__start"
    id="taskbar-start"
    aria-label={startLabel}
    aria-expanded="false"
    aria-haspopup="dialog"
    aria-controls="start-menu"
    type="button"
  >
    <span class="taskbar__start-icon" aria-hidden="true">⚡</span>
    <span class="taskbar__start-label">Albert.exe</span>
  </button>

  <!-- Center: Open windows -->
  <div
    id="taskbar-windows"
    class="taskbar__windows"
    role="list"
    aria-label={lang === 'fr' ? 'Fenêtres ouvertes' : 'Open windows'}
  ></div>

  <!-- Right: Lang + Clock -->
  <div class="taskbar__right">
    <a
      href={alternateLangPath}
      class="taskbar__lang"
      aria-label={alternateLangAriaLabel}
      lang={alternateLangLabel.toLowerCase()}
    >
      {alternateLangLabel}
    </a>
    <time
      id="taskbar-clock"
      class="taskbar__clock"
      aria-live="polite"
      aria-label={lang === 'fr' ? 'Heure actuelle' : 'Current time'}
    ></time>
  </div>
</nav>

<style>
/* ── Start Menu ────────────────────────────────────────────────── */
.start-menu {
  position: fixed;
  bottom: 52px;
  left: 0.5rem;
  width: 240px;
  background: rgba(8, 4, 20, 0.97);
  backdrop-filter: blur(24px);
  -webkit-backdrop-filter: blur(24px);
  border: 1px solid rgba(139, 92, 246, 0.35);
  border-radius: 0.625rem;
  z-index: 1100;
  box-shadow: 0 -8px 32px rgba(0, 0, 0, 0.6), 0 0 0 1px rgba(139, 92, 246, 0.1);
  overflow: hidden;
  animation: start-menu-in 0.15s ease;
}

.start-menu[hidden] { display: none; }

@keyframes start-menu-in {
  from { opacity: 0; transform: translateY(8px); }
  to   { opacity: 1; transform: translateY(0); }
}

.start-menu__header {
  display: flex;
  align-items: center;
  gap: 0.75rem;
  padding: 0.875rem 1rem;
  background: linear-gradient(135deg, rgba(139, 92, 246, 0.2) 0%, rgba(139, 92, 246, 0.06) 100%);
  border-bottom: 1px solid rgba(139, 92, 246, 0.2);
}

.start-menu__avatar {
  width: 2.25rem;
  height: 2.25rem;
  border-radius: 50%;
  background: rgba(139, 92, 246, 0.2);
  border: 1px solid rgba(139, 92, 246, 0.4);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1.1rem;
  flex-shrink: 0;
}

.start-menu__name {
  margin: 0;
  font-size: 0.8125rem;
  font-weight: 700;
  color: #FAF5FF;
  line-height: 1.2;
}

.start-menu__role {
  margin: 0.15rem 0 0;
  font-size: 0.65rem;
  color: #8B5CF6;
  font-weight: 500;
}

.start-menu__section {
  list-style: none;
  padding: 0.375rem 0;
  margin: 0;
}

.start-menu__section-label {
  font-size: 0.6rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  color: #475569;
  padding: 0.375rem 1rem 0.125rem;
}

.start-menu__section--legal {
  padding-bottom: 0.375rem;
}

.start-menu__item {
  display: flex;
  align-items: center;
  gap: 0.625rem;
  width: 100%;
  padding: 0.45rem 1rem;
  background: transparent;
  border: none;
  color: #C4B5FD;
  font-size: 0.8rem;
  font-weight: 500;
  cursor: pointer;
  font-family: inherit;
  text-decoration: none;
  transition: background 0.12s ease, color 0.12s ease;
  text-align: left;
}

.start-menu__item:hover {
  background: rgba(139, 92, 246, 0.15);
  color: #FAF5FF;
}

.start-menu__item:focus-visible {
  outline: 2px solid #F59E0B;
  outline-offset: -2px;
}

.start-menu__item--legal {
  color: #94A3B8;
  font-size: 0.75rem;
}

.start-menu__item--legal:hover {
  color: #C4B5FD;
}

.start-menu__item-icon {
  font-size: 0.875rem;
  flex-shrink: 0;
}

.start-menu__sep {
  height: 1px;
  background: rgba(139, 92, 246, 0.15);
  margin: 0;
}

.start-menu__footer {
  display: flex;
  align-items: center;
  gap: 0.375rem;
  padding: 0.5rem 1rem;
  font-size: 0.6rem;
  color: #475569;
  background: rgba(0, 0, 0, 0.2);
  border-top: 1px solid rgba(139, 92, 246, 0.1);
  font-family: 'Consolas', monospace;
}

/* ── Taskbar ───────────────────────────────────────────────────── */
.taskbar {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  height: 48px;
  background: rgba(8, 4, 20, 0.92);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  border-top: 1px solid rgba(139, 92, 246, 0.2);
  display: flex;
  align-items: center;
  gap: 0.75rem;
  padding: 0 0.75rem;
  z-index: 1000;
}

.taskbar__start {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  background: rgba(139, 92, 246, 0.15);
  border: 1px solid rgba(139, 92, 246, 0.3);
  color: #FAF5FF;
  padding: 0.375rem 0.875rem;
  border-radius: 0.375rem;
  cursor: pointer;
  font-size: 0.8125rem;
  font-weight: 600;
  transition: background 0.2s ease, border-color 0.2s ease;
  font-family: inherit;
  flex-shrink: 0;
}

.taskbar__start:hover {
  background: rgba(139, 92, 246, 0.3);
  border-color: rgba(139, 92, 246, 0.5);
}

.taskbar__start:focus-visible {
  outline: 2px solid #F59E0B;
  outline-offset: 2px;
}

.taskbar__start-icon {
  font-size: 1rem;
}

.taskbar__windows {
  flex: 1;
  display: flex;
  align-items: center;
  gap: 0.375rem;
  overflow-x: auto;
  min-width: 0;
  /* hide scrollbar */
  scrollbar-width: none;
  -ms-overflow-style: none;
}

.taskbar__windows::-webkit-scrollbar {
  display: none;
}

.taskbar__right {
  display: flex;
  align-items: center;
  gap: 0.75rem;
  margin-left: auto;
  flex-shrink: 0;
}

.taskbar__lang {
  font-size: 0.75rem;
  font-weight: 700;
  color: #F59E0B;
  text-decoration: none;
  padding: 0.25rem 0.5rem;
  border-radius: 0.25rem;
  border: 1px solid rgba(245, 158, 11, 0.3);
  transition: all 0.2s ease;
  letter-spacing: 0.03em;
}

.taskbar__lang:hover {
  background: rgba(245, 158, 11, 0.1);
  border-color: rgba(245, 158, 11, 0.5);
  color: #FCD34D;
}

.taskbar__lang:focus-visible {
  outline: 2px solid #F59E0B;
  outline-offset: 2px;
}

.taskbar__clock {
  font-size: 0.8125rem;
  color: #C4B5FD;
  font-variant-numeric: tabular-nums;
  min-width: 5rem;
  text-align: right;
  font-family: 'JetBrains Mono', 'Fira Code', monospace;
}
</style>

<script>
(function () {
  // ── Live clock ──────────────────────────────────────────────────────────────
  const clockEl = document.getElementById('taskbar-clock') as HTMLTimeElement | null;

  function updateClock(): void {
    if (!clockEl) return;
    const now = new Date();
    const timeStr = now.toLocaleTimeString([], { hour: '2-digit', minute: '2-digit', second: '2-digit' });
    clockEl.textContent = timeStr;
    clockEl.setAttribute('datetime', now.toISOString());
  }

  updateClock();
  setInterval(updateClock, 1000);

  // ── Start Menu ───────────────────────────────────────────────────────────────
  const startBtn  = document.getElementById('taskbar-start');
  const startMenu = document.getElementById('start-menu');
  if (!startBtn || !startMenu) return;

  function openMenu(): void {
    startMenu.hidden = false;
    startBtn!.setAttribute('aria-expanded', 'true');
    // Focus first item
    const first = startMenu.querySelector<HTMLElement>('.start-menu__item');
    first?.focus();
  }

  function closeMenu(): void {
    startMenu.hidden = true;
    startBtn!.setAttribute('aria-expanded', 'false');
  }

  startBtn.addEventListener('click', () => {
    if (startMenu.hidden) openMenu(); else closeMenu();
  });

  // Close on outside click
  document.addEventListener('click', (e) => {
    if (!startMenu.hidden && !startMenu.contains(e.target as Node) && e.target !== startBtn) {
      closeMenu();
    }
  });

  // Close on Escape
  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape' && !startMenu.hidden) {
      closeMenu();
      startBtn.focus();
    }
  });

  // Open window items in start menu
  startMenu.addEventListener('click', async (e) => {
    const btn = (e.target as HTMLElement).closest<HTMLElement>('[data-start-open]');
    if (!btn) return;
    const id = btn.dataset['startOpen'];
    if (!id) return;
    closeMenu();
    const { open } = await import('../../utils/window-manager');
    open(id);
  });
})();
</script>
